# String de conexão com o PostgreSQL local via Docker
DATABASE_URL="postgres://postgres:postgrespassword@localhost:5432/salgado_care"

# Porta do servidor da API REST
PORT=3001

# URL do Frontend autorizada no CORS
FRONTEND_URL="http://localhost:3000"
